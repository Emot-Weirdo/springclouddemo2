
spring:
  application:
    name: nativeconfigserver
  profiles:
    active: native #配置⽂件的获取⽅式
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared #本地配置⽂件存放的路径
server:
  port: 8762
